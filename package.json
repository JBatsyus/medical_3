{
  "name": "medical",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
  "sass": "sass styles/main.scss:styles/main.css",
  "prefix": "postcss styles/main.css --use autoprefixer --replace",
  "build": "npm run sass && npm run prefix",
  "watch:sass": "sass --watch styles/main.scss:styles/main.css",
  "watch:prefix": "postcss styles/main.css --use autoprefixer -o styles/main.css --watch",
  "dev": "run-p watch:sass watch:prefix"
},
  "author": "Btss",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "sass": "^1.80.6"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
